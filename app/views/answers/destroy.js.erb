<% if @answer.persisted? %>
  $('.answer-<%= @answer.id %> .answer-attached-files').html('<%= render 'shared/files', resource: @answer %>');
<% else %>
  $('.answer-<%= @answer.id %>').remove();
<% end %>

<%= render 'shared/update_flash' %>
